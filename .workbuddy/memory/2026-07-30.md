# 2026-07-30

## Chapter 5 Conversion Completed
- Converted **第 5 章 通用运算放大器 IC 的分析** (PDF pages 151-170, 20 pages) to mdbook-compatible markdown
- Produced 27 figures + 1 table (28 image assets) via automated band-detection + visual coordinate verification
- Created 5 source-sections, chapter-05.md (411 lines), verification.md, crop script, build script
- Updated SUMMARY.md to include chapter 5 entry
- Key technical approach: Read tool for page transcription (Windows OCR unavailable in this env), PIL-based ink-band detection for figure coordinates with annotated preview verification

## Circuit-symbol math formatting applied to Chapter 5
- User requested all inline circuit symbols (Q_, V_, I_, R_, C_, D_, J_, Z_, P_, N_, g_m, h_FE, τ_F, f_T) wrapped in `\(...\)` MathJax, matching their manual edit of 5.1.4.
- Built `tmp/wrap_symbols.py` (regex transform that protects existing math/comments/alt-text) and applied to all 5 source-sections; regenerated chapter-05.md.
- Established project convention: circuit symbols in body text → inline math; type acronyms (FET/BJT/NPN/PNP/CMOS) & units stay plain. Recorded in MEMORY.md.

## Math delimiter bug fix + build verification (resumed session)
- User reported inline/display math MUST use double backslash in markdown source: `\\(` `\\(` and `\\[` `\\]` (single `\(` is wrong for mdbook). Confirmed via `repr()` that chapters 1-4 use double-backslash; chapter-05 source had single. Fixed by replacing `\(`→`\\(`, `\)`→`\\)`, `\[`→`\\[`, `\]`→`\\]` in all 5 source-sections, regenerated chapter-05.md (now 175 `\\(`, 21 `\\[`).
- `mdbook build` had transiently failed with "Access is denied" removing stale `book/chapter-02` (file lock). Re-ran successfully: clean build, "HTML book written".
- Verified `book/chapter-05/chapter-05.html`: MathJax 2.7.1 loader (`TeX-AMS-MML_HTMLorMML`) registers `\(`/`\[` delimiters; 175 inline + 21 display math blocks present; 5.1.4 example paragraph matches user's manual edit exactly. Convention confirmed correct.
